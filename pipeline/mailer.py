import os
import smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart


def send_digest():
    # Build daily digest from updated joblist
    # Format new roles and status changes
    # Send email via MAIL_FROM / MAIL_TO / MAIL_PASSWORD
    pass


if __name__ == "__main__":
    send_digest()
